<!-- GENERATED by tools/findings.sh from docs/review/findings/*.md.
     Do not edit. On a merge conflict, regenerate instead of merging. -->

# FINDINGS — elke reviewbevinding, één regel elk

129 bevindingen uit 14 reviews: 6 blocker, 25 major, 56 minor, 28 nit, 14 question. **129 zonder Resolution-regel.**

| Sev | Slug | ID | Bevinding | Cat | Zekerheid | Waar | Resolution |
|---|---|---|---|---|---|---|---|
| blocker | `ar-sessions` | F01 | `db:migrate` reports success without a word while leaving the app unable to serve a single request | correctness | confirmed | `db/migrate/20240929111106_add_sessions_table.rb`, and the deploy note in `docs/features/ar-sessions/status.md` / `docs/REGISTER.md` | **—** |
| blocker | `ldap-mail-prefs` | F01 | A destructive bulk overwrite with no dry-run and no record of the previous values | correctness | confirmed | `lib/tasks/disable_mail_ldap_users.rake:28-32` | **—** |
| blocker | `ldap-mail-prefs` | F02 | It does not select LDAP-only users: it selects "in this one group and no other", which also catches admins, locked, unactivated and local-password accounts | correctness | confirmed | `lib/tasks/disable_mail_ldap_users.rake:21-27` | **—** |
| blocker | `mypage-query-blocks` | F01 | The locales patch does not apply to current `origin/master` | conventions (patch hygiene) | confirmed | `patches/mypage-query-blocks/2026-09-03-r24882-locales.patch`, hunk `config/locales/fr.yml:1542` | **—** |
| blocker | `wiki-export-attachments` | F01 | `patch/wiki-export-attachments` implements the design Jan rejected; the dossier, the `.patch` files and the GEOxyz commit implement the one he chose | dossier | confirmed | `origin/patch/wiki-export-attachments` @ `2cb6231c7` vs `patches/wiki-export-attachments/2026-09-01-r24882-feature.patch` and `7.0-stable-GEOxyz` @ `28c618860` | **—** |
| blocker | `wiki-export-attachments` | F02 | An attachment named `<PageTitle>.txt` produces two ZIP entries at the same path, and the page's own source text silently disappears from the archive | correctness | confirmed | `app/controllers/wiki_controller.rb` — `wiki_pages_to_zip` (the `archived_file_names = []` reset inside the per-page loop) and `archived_attachment_filename` | **—** |
| major | `ar-sessions` | F02 | The two deploy notes describe two different production databases, and the difference decides whether the old session rows are dangerous | backward-compat | confirmed (the contradiction; the production state itself is unverifiable from here) | `docs/features/ar-sessions/status.md` ("Wat Jan nog moet doen"), mirrored in `docs/REGISTER.md` | **—** |
| major | `ar-sessions` | F03 | A `sessions` row whose `session_id` is stored in plain text is accepted as a valid login, so the "reading the table gives you nothing" claim only holds for rows this gem wrote | security | confirmed (mechanism); the precondition — that production has such rows — is unverified | `config/application.rb:104` (`:active_record_store`, no `secure_session_only`) | **—** |
| major | `ar-sessions` | F04 | The table grows with request volume rather than with logins, and the change ships neither a cron entry nor a chosen threshold | performance | confirmed | `config/application.rb:104`; deploy note in `status.md` / `docs/REGISTER.md` | **—** |
| major | `assignee-nobody` | F01 | the NULL folding returns an unparenthesised `OR`, and one core filter splices it into an `AND`, giving wrong rows | correctness | confirmed | `app/models/query.rb:1290` (the `=` branch) and `app/models/query.rb:1302` | **—** |
| major | `gitignore-credentials` | F01 | Per-environment credential keys (`config/credentials/<env>.key`) are not ignored, so a production master key can still be committed | security | confirmed | `.gitignore:11` and `.gitignore:14` | **—** |
| major | `imap-oauth` | F01 | The dossier's "no credential in any string" claim is false as written, and it is the claim the note leads with | dossier | confirmed | `docs/features/imap-oauth/dossier.md:388` (the claim) against `lib/tasks/email.rake:193` (the code) | **—** |
| major | `ldap-mail-prefs` | F03 | No transaction: a mid-run failure leaves a partial write, and the error does not say which user it died on | correctness | confirmed | `lib/tasks/disable_mail_ldap_users.rake:24-36` | **—** |
| major | `ldap-mail-prefs` | F04 | All the logic lives inline in a `.rake` file, so it is neither linted nor testable, and Redmine does not do it that way | conventions | confirmed | `lib/tasks/disable_mail_ldap_users.rake:18-38` | **—** |
| major | `ldap-mail-prefs` | F05 | No test at all for the UPDATE/SKIP predicate that decides whose mail gets muted | test-quality | confirmed | `lib/tasks/disable_mail_ldap_users.rake` (no corresponding file under `test/`) | **—** |
| major | `members-pagination` | F01 | The note attributes to Takenori's patch a behaviour unpatched Redmine already has everywhere | dossier | confirmed | `docs/features/members-pagination/dossier.md` — "The problem" | **—** |
| major | `members-pagination` | F02 | The English text prepared for the note has no thanks and no confirmation, only the defect | dossier | confirmed | `docs/features/members-pagination/dossier.md` — "The finding", | **—** |
| major | `members-pagination` | F03 | The screenshot attached as the note's evidence does not show what the note says it shows | dossier | confirmed | `docs/features/members-pagination/shots/defect-empty-page-after-delete.png`, | **—** |
| major | `mypage-query-blocks` | F02 | `before-select-at-default-maximum.png` and `select-at-default-maximum.png` are not the same image, and the "after" one has a stray "Delete" tooltip in it | dossier | confirmed | `docs/features/mypage-query-blocks/shots/{before-,}select-at-default-maximum.png`; the claim is in `dossier.md` ("Anticipated objections", row 1: "are the same picture") and in `status.md` ("Wat Jan nog moet doen": "het voor/na-paar … is dezelfde afbeelding") | **—** |
| major | `mypage-query-blocks` | F03 | The clamp to a minimum of 1 is justified with a reason that is not true, and it removes the setting value that answers note-5 best | correctness | confirmed | `lib/redmine/my_page.rb:71-73`; the justification is in `dossier.md` ("Proposed change", the input table's `0` row, and the "Anticipated objections" table) and in the commit message of `ec6466159` | **—** |
| major | `revision-branches` | F01 | An invalid regular expression in `revision_branches_excluded` is silently accepted and stored; the core pattern this is modelled on rejects it at the form | conventions | confirmed | `app/models/setting.rb:161-186` (`validate_all_from_params`, unmodified by the patch); `app/models/changeset.rb:243-255`; `app/views/settings/_repositories.html.erb:62-69` | **—** |
| major | `revision-branches` | F02 | One `git branch --contains` subprocess per associated revision, with nothing bounding the count | performance | confirmed | `app/views/issues/tabs/_changesets.html.erb:23-26`; `app/models/changeset.rb:204-210`; `app/controllers/issues_controller.rb:257` | **—** |
| major | `revision-branches` | F03 | The dossier defends the cost with "the revision page already calls three Git commands"; the revision page calls none | dossier | confirmed | `docs/features/revision-branches/dossier.md`, "Anticipated objections", first row ("the revision page already calls three Git commands (note 20) and is already in `robots.txt`") | **—** |
| major | `search-token-limit` | F01 | The `Any searchable text` filter still drops keywords after the fifth, and the dossier says every text filter is fixed | dossier | confirmed | `app/models/issue_query.rb:876` (`sql_for_any_searchable_field`), `app/models/issue_query.rb:295`, `app/models/query.rb:343`; dossier sections "The problem" and "Proposed change" | **—** |
| major | `search-token-limit` | F02 | After the patch, the global search page's "Apply issues filter" button can land on an empty issue list for a search it just reported results for | correctness | confirmed | `app/helpers/search_helper.rb:73` (`issues_filter_path`), consumed at `app/views/search/index.html.erb:49` | **—** |
| major | `search-token-limit` | F03 | The performance answer carries no numbers, and the argument it does make does not hold for the OR operators | performance | confirmed (measurements below are mine; the `unaccent` remark is reasoned, not measured) | dossier, "Anticipated objections", row *"Unbounded tokens in a filter can be slow."*; code path `app/models/query.rb:1518` → `lib/redmine/database.rb:73` | **—** |
| major | `version-subprojects` | F01 | The "six to four" figure the note is built on is only true for a user who cannot see the private projects; an admin sees six to five | dossier | confirmed | `docs/features/version-subprojects/status.md` ("Wat Jan nog moet doen"), | **—** |
| major | `version-subprojects` | F02 | The dossier answers "does this leak version names you cannot see?" with "No", and that is wrong for the filter as a whole — it is also v2's best counter-argument | dossier | confirmed | `docs/features/version-subprojects/dossier.md`, "Anticipated objections" table, the | **—** |
| major | `webhook-tracker-filter` | F01 | Neither patch file applies to current trunk; the dossier's "#44386 does not conflict" claim is false | conventions | confirmed | `patches/webhook-tracker-filter/2026-09-03-r24882-feature.patch`, `patches/webhook-tracker-filter/2026-09-03-r24882-locales.patch`; the hunk at `app/models/webhook.rb:118` | **—** |
| major | `webhook-tracker-filter` | F02 | `Tracker` never gets the inverse association, so deleting a tracker silently un-restricts every hook that used it | correctness | confirmed | `app/models/webhook.rb:93` (the new `has_and_belongs_to_many :trackers`); the missing counterpart in `app/models/tracker.rb` | **—** |
| major | `wiki-export-attachments` | F03 | An attachment named after a child page collides with that child's directory, and `unzip` then refuses to extract the child page's source | correctness | confirmed | `app/controllers/wiki_controller.rb` — `wiki_pages_to_zip` / `wiki_page_directories` | **—** |
| minor | `ar-sessions` | F05 | `db:rollback` on this migration reports "reverted" and does nothing, then leaves the app 500-ing until someone re-migrates | correctness | confirmed | `db/migrate/20240929111106_add_sessions_table.rb:3` | **—** |
| minor | `ar-sessions` | F06 | The boot failure is real and loud, but the error message quoted in the note does not exist in Rails 8.1 | dossier | confirmed | `docs/features/ar-sessions/status.md` (deploy note 1), `docs/REGISTER.md` | **—** |
| minor | `ar-sessions` | F07 | Session data is `Marshal`-loaded from the database, so database write access becomes code execution in the Redmine process | security | confirmed (mechanism), speculative as an exploit path (needs write access) | `config/application.rb:104` (no `ActiveRecord::SessionStore.serializer` set) | **—** |
| minor | `ar-sessions` | F08 | `t.text` is unlimited on PostgreSQL but 64 KB on MySQL, where an oversized session becomes a 500 instead of a truncation | portability | confirmed (mechanism), unverified for GEOxyz (adapter unknown to me) | `db/migrate/20240929111106_add_sessions_table.rb:7` | **—** |
| minor | `ar-sessions` | F09 | Nothing in the suite asserts that sessions are in the database, and the 65 functional test files never touch the store at all | test-quality | confirmed | `the commit adds no test` | **—** |
| minor | `ar-sessions` | F10 | The one committed screenshot would look identical with the old store, and there is no before/after pair | dossier | confirmed | `docs/features/ar-sessions/shots/logged-in-with-a-database-session.png`, `verify/ar-sessions.mjs` | **—** |
| minor | `assignee-nobody` | F02 | the "currently has no assignee" half of `ev`/`!ev` is untested: deleting it keeps the suite green | test-quality | confirmed | `test/unit/query_test.rb` — `test_operator_has_been_nobody`, | **—** |
| minor | `assignee-nobody` | F03 | neither of the two gates is pinned by a test, including the `is_custom_filter` guard the patch's safety argument rests on | test-quality | confirmed | `app/models/query.rb:1248-1251` | **—** |
| minor | `assignee-nobody` | F04 | the dossier explains the adapted `assigned_to_values[1..]` test wrongly, and that sentence is going onto redmine.org | dossier | confirmed | `docs/features/assignee-nobody/dossier.md`, "One existing test | **—** |
| minor | `assignee-nobody` | F05 | six other core filters silently gain `none` semantics, with no test and no way to reach them from the UI | backward-compat | confirmed | `app/models/query.rb:1248-1251` | **—** |
| minor | `geoxyz-hosts` | F01 | The regexp is case-sensitive, so an uppercase `Host` header is refused | correctness | confirmed | `config/environments/development.rb:63` | **—** |
| minor | `gitignore-credentials` | F02 | Rails appends its own ignore block on top of these lines, dirtying a tracked file | conventions | confirmed | `.gitignore:14` | **—** |
| minor | `imap-oauth` | F02 | The IMAP connection is opened, then held idle for up to two minutes while the token is fetched | correctness | probable — the ordering is confirmed by reading; the consequence I did not execute | `lib/redmine/imap.rb:40-46` | **—** |
| minor | `imap-oauth` | F03 | A 200 response with a non-JSON body escapes as a raw JSON::ParserError, unlike every other failure in the class | correctness | confirmed | `lib/redmine/oauth2_client.rb:131` | **—** |
| minor | `imap-oauth` | F04 | The happy-path test passes against production code that discards the HTTP response | test-quality | confirmed | `test/unit/lib/redmine/oauth2_client_test.rb:252-259` | **—** |
| minor | `ldap-mail-prefs` | F06 | The output prints `UPDATE:` for users it did not change, so the log is not a record of what the run did | correctness | confirmed | `lib/tasks/disable_mail_ldap_users.rake:34` | **—** |
| minor | `ldap-mail-prefs` | F07 | An empty or unpopulated group is a silent exit-0 no-op with no output at all | correctness | confirmed | `lib/tasks/disable_mail_ldap_users.rake:22-24` | **—** |
| minor | `ldap-mail-prefs` | F08 | The `desc` says "Mute the mail notifications", but `only_assigned` still sends mail | conventions | confirmed | `lib/tasks/disable_mail_ldap_users.rake:19` | **—** |
| minor | `ldap-mail-prefs` | RUN 2 (idempotency) |  |  |  |  | **—** |
| minor | `ldap-mail-prefs` | empty group |  |  |  |  | **—** |
| minor | `members-pagination` | F04 | Two mechanism sentences in the note text are wrong | correctness | confirmed | `docs/features/members-pagination/dossier.md` — "The finding", | **—** |
| minor | `members-pagination` | F05 | "leaving the tab recovers it" is not true, which understates the finding | correctness | probable (read, not driven in a browser) | `docs/features/members-pagination/dossier.md` — "The finding" | **—** |
| minor | `members-pagination` | F06 | The clamp corrects the paginator but not `params[:members_page]`, so the address bar and every row link keep the dead page | correctness | confirmed | `app/helpers/members_helper.rb` and `app/helpers/groups_helper.rb` | **—** |
| minor | `members-pagination` | F07 | The RuboCop evidence is not reproducible: 4 offences on those files, not 0 | dossier | confirmed | `dossier.md` and `status.md` — "RuboCop op de 10 gewijzigde | **—** |
| minor | `members-pagination` | F08 | The committer-identity count in `status.md` does not match the branch | dossier | confirmed | `docs/features/members-pagination/status.md` — "Elf van de veertien | **—** |
| minor | `mypage-query-blocks` | F04 | The asynchronous-loading measurement table is not reproducible as presented, and one sentence double-counts the baseline | dossier | confirmed | `dossier.md`, section "What asynchronous loading would and would not fix" | **—** |
| minor | `mypage-query-blocks` | F05 | `test_page_should_render_issuequery_blocks_over_a_lowered_maximum` does not discriminate the change, but the dossier lists it as proof | test-quality | confirmed | `test/functional/my_controller_test.rb:256-273`; the claim is in `dossier.md`'s "Tests" table and in the "Anticipated objections" row about lowered limits | **—** |
| minor | `mypage-query-blocks` | F06 | `MyPage.max_occurs` is new public API with no unit test, an unguarded `nil` dereference, and a silently changed return type on `MyPage.blocks` | backward-compat | confirmed | `lib/redmine/my_page.rb:68-74` | **—** |
| minor | `mypage-query-blocks` | F09 | Every screenshot shows an *unconfigured* query block, so the feature's actual payload was never seen in a browser | dossier | confirmed | `docs/features/mypage-query-blocks/shots/*.png`; `tools/dev-seed.rb` | **—** |
| minor | `revision-branches` | F04 | The feature also appears, and runs `git`, on the diff page — which the setting name, the dossier and the G9 evidence all omit | ui | confirmed | `app/views/repositories/_changeset.html.erb:23-28`; the partial is rendered by `app/views/repositories/revision.html.erb:29` **and** `app/views/repositories/diff.html.erb:29` | **—** |
| minor | `revision-branches` | F05 | The four settings are offered on installations where Git is not an enabled SCM, and nothing in the UI says the feature is Git-only | scm-symmetry | confirmed | `app/views/settings/_repositories.html.erb:59-69`; `config/locales/en.yml:471-474` | **—** |
| minor | `revision-branches` | F06 | The hint under the exclusion field shows a regular-expression example, but the field is glob syntax unless the box below it is ticked | ui | confirmed | `app/views/settings/_repositories.html.erb:67-68` | **—** |
| minor | `revision-branches` | F07 | `test_show_changesets_tab_should_not_display_branches_without_view_changesets_permission` passes with the entire feature removed | test-quality | confirmed | `test/functional/issues_controller_test.rb:3360-3374` | **—** |
| minor | `revision-branches` | F08 | The note-20 answer holds for crawlers that do not run JavaScript; it does not hold for the ones that do | dossier | confirmed | `docs/features/revision-branches/dossier.md`, "Proposed change" and "Anticipated objections", second row; `app/views/common/_tabs.html.erb:31`; `app/helpers/issues_helper.rb:737-748` | **—** |
| minor | `search-token-limit` | F04 | `tools/check-patch-clean.sh` fails today, and the note would name a trunk revision 155 revisions old | dossier | confirmed | dossier "Made against: `origin/master` r24882 (2026-08-03)" and the `check-patch-clean.sh: PASS` line; `status.md` "Bewijs" | **—** |
| minor | `search-token-limit` | F05 | The key `~` test asserts only emptiness, which is the weaker half of the behaviour it is proving | test-quality | confirmed | `test/unit/query_test.rb:3438` (`test_sql_contains_should_not_limit_the_number_of_tokens`) | **—** |
| minor | `version-subprojects` | F03 | A version shared `descendants` from a project between the query's project and a selected subproject is still not offered, so the "matches what the query returns" framing overclaims | correctness | confirmed | `app/models/query.rb:660-666` | **—** |
| minor | `version-subprojects` | F04 | "The union costs one extra query" is four; measured six SQL statements against trunk's two | performance | confirmed | `docs/features/version-subprojects/dossier.md`, "Anticipated objections" row | **—** |
| minor | `version-subprojects` | F05 | On `queries/new` and `queries/edit` the new JavaScript sends the session's CSRF token in a GET query string, where Redmine logs it unfiltered | security | confirmed | `app/assets/javascripts/application-legacy.js:178-181` | **—** |
| minor | `version-subprojects` | F06 | Handing the whole `params` hash to `build_from_params` makes the endpoint's own `name` argument double as a `ProjectQuery` filter value | correctness | confirmed | `app/controllers/queries_controller.rb:103` | **—** |
| minor | `version-subprojects` | F07 | Three of the six new tests are Go MAEDA's, near-verbatim, and neither the dossier nor the note says so | dossier | confirmed | `test/unit/query_test.rb:172-217`, `test/functional/queries_controller_test.rb:826-845` | **—** |
| minor | `version-subprojects` | F08 | Every new test asserts membership in the value list; none asserts the newly offered value actually returns the issue, and `TimeEntryQuery` is claimed but untested | test-quality | confirmed | `test/unit/query_test.rb:164-217` | **—** |
| minor | `webhook-issue-closed` | F01 | The dossier's "No query is added on any path" is false: a closing save runs `hooks_for` twice when webhooks are enabled | dossier | confirmed | `docs/features/webhook-issue-closed/dossier.md`, "Anticipated objections", the row *"What does this cost on `Issue#save` in an installation with webhooks off…"* — final sentence | **—** |
| minor | `webhook-issue-closed` | F02 | `issue.closed` is silently lost when the same issue is saved twice inside one transaction | correctness | confirmed (mechanism), speculative (that any real installation hits it) | `app/models/concerns/issue/webhookable.rb:24` | **—** |
| minor | `webhook-issue-closed` | F03 | "Every row of the transition table has a test" is not true for two and a half of the eight rows | test-quality | confirmed | `docs/features/webhook-issue-closed/status.md` ("**De tabel met de acht overgangen** … Elke rij heeft een test") and `dossier.md` ("Six of the eight are one row of the transition table each") | **—** |
| minor | `webhook-issue-closed` | F04 | The locale argument has gone stale: `fr.yml` in current trunk has the sibling keys translated | i18n | confirmed | `docs/features/webhook-issue-closed/dossier.md`, "Locales — why `en.yml` only"; the same facts in `status.md` and in the K-09 correction in `docs/DECISIONS.md` | **—** |
| minor | `webhook-issue-closed` | F05 | `closed` is kept out of the generic callback `case` on layering grounds, then put into the generic timestamp `case` anyway | conventions | confirmed | `lib/redmine/acts/webhookable.rb:75` (the `when 'updated', 'closed'` branch) against `app/models/concerns/issue/webhookable.rb:24` | **—** |
| minor | `webhook-tracker-filter` | F03 | The French "one word could not be derived" caveat is factually wrong | i18n | confirmed | `docs/features/webhook-tracker-filter/dossier.md`, the `fr` row of the translation table; `docs/features/webhook-tracker-filter/decisions.md` | **—** |
| minor | `webhook-tracker-filter` | F04 | The dossier's "the sibling hints are still English" note, and the K-08 rationale, are out of date for French | dossier | confirmed | `docs/features/webhook-tracker-filter/dossier.md`, the "Honest note on `nl`, `fr` and `es`" paragraph and the last row of the objections table | **—** |
| minor | `webhook-tracker-filter` | F05 | The N+1 figures in the dossier do not reproduce, and the preload costs one query in the common case | performance | confirmed | `docs/features/webhook-tracker-filter/dossier.md`, the N+1 measurement table; `app/models/webhook.rb:129` | **—** |
| minor | `webhook-tracker-filter` | F06 | Nothing tests that unchecking every tracker clears the selection, or that the edit form pre-checks it | test-quality | confirmed | `test/functional/webhooks_controller_test.rb:74-90`; `app/views/webhooks/_form.html.erb:48` (the blank `hidden_field_tag`) | **—** |
| minor | `webhook-tracker-filter` | F07 | The Trackers fieldset is offered on hooks that subscribe to no issue event, where it does nothing | ui | confirmed | `app/views/webhooks/_form.html.erb:43-50` | **—** |
| minor | `wiki-export-attachments` | F04 | `archived_attachment_filename` re-implements `Attachment.archive_attachments`' rename loop line for line, and the dossier says nothing was copied | minimality | confirmed | `app/controllers/wiki_controller.rb` — `archived_attachment_filename`; compare `app/models/attachment.rb` — `Attachment.archive_attachments` | **—** |
| minor | `wiki-export-attachments` | F05 | `test_export_to_zip` now computes its expected paths with a test-side copy of the production algorithm, and its set-equality assertion was weakened to a count | test-quality | confirmed | `test/functional/wiki_controller_test.rb` — `test_export_to_zip` and the new private `wiki_page_titles_to_root` | **—** |
| minor | `wiki-export-attachments` | F06 | No test exercises an attachment on a nested page, which is the combination the feature exists for | test-quality | confirmed | `test/functional/wiki_controller_test.rb` — `test_export_to_zip_with_attachments` and the four other `with_attachments` tests | **—** |
| minor | `wiki-export-attachments` | F07 | A page whose parent is not in the exported collection is silently dropped instead of exported | correctness | probable | `app/controllers/wiki_controller.rb` — `wiki_pages_to_zip` / `wiki_page_directories(pages.group_by(&:parent_id))` | **—** |
| nit | `ar-sessions` | F11 | `status.md`'s RuboCop evidence lists a file this commit does not touch | dossier | confirmed | `docs/features/ar-sessions/status.md` ("RuboCop op de gewijzigde bestanden: 0 offences op 4 bestanden") | **—** |
| nit | `assignee-nobody` | F06 | the two equivalence tests would pass on two empty sets | test-quality | confirmed | `test/unit/query_test.rb` — | **—** |
| nit | `assignee-nobody` | F07 | five of the seven "before" screenshots are the same file, and the dossier describes them as showing something they cannot show | dossier | confirmed | `docs/features/assignee-nobody/shots/`, and the "Failure paths | **—** |
| nit | `assignee-nobody` | F08 | "all 63 locale files" — trunk has 50 | dossier | confirmed | `docs/features/assignee-nobody/dossier.md`, "Proposed change" and | **—** |
| nit | `geoxyz-hosts` | F02 | The matched set is wider than "hostnames", but not in a way that re-opens DNS rebinding | security | confirmed | `config/environments/development.rb:63` | **—** |
| nit | `geoxyz-hosts` | F03 | The line lives in an upstream-tracked file, so it is a standing merge-conflict candidate | conventions | confirmed | `config/environments/development.rb:63` | **—** |
| nit | `ldap-mail-prefs` | F09 | `find_by(:lastname => ...)` is case-sensitive; Redmine has a `Group.named` scope for exactly this lookup | conventions | confirmed | `lib/tasks/disable_mail_ldap_users.rake:21` | **—** |
| nit | `ldap-mail-prefs` | F10 | Two different idioms for writing the same object's preferences, three lines apart | conventions | confirmed | `lib/tasks/disable_mail_ldap_users.rake:30-31` | **—** |
| nit | `members-pagination` | N01 | The clamp's comment and its arithmetic are duplicated verbatim in two helpers | minimality | confirmed | `app/helpers/members_helper.rb`, `app/helpers/groups_helper.rb` in | **—** |
| nit | `mypage-query-blocks` | F07 | The `config/settings.yml` comment restates the setting's own label | minimality | confirmed | `config/settings.yml:92` | **—** |
| nit | `mypage-query-blocks` | F08 | One locale citation in the dossier is off by one line | i18n | confirmed | `dossier.md`, translations table, `en` row | **—** |
| nit | `revision-branches` | F09 | `names.sort!` can raise where the rest of the method carefully returns `[]` | correctness | probable | `lib/redmine/scm/adapters/git_adapter.rb:97-113` | **—** |
| nit | `revision-branches` | F10 | The commit message is far longer than anything in trunk's history | conventions | confirmed | commit `181dda224`; `patches/revision-branches/2026-09-03-r24882-feature.patch:1-40` | **—** |
| nit | `search-token-limit` | F06 | The comment moved into `Fetcher` restates the line and drops the one thing worth writing down | conventions | confirmed | `lib/redmine/search.rb:59-60` | **—** |
| nit | `search-token-limit` | F07 | The dossier's GEOxyz evidence says six changed Ruby files; the commit changes four | dossier | confirmed | `dossier, "Evidence (INV-8)", the RuboCop bullet` | **—** |
| nit | `search-token-limit` | F08 | Statement-length ceiling on SQLite is theoretically reachable through a saved query; bind-parameter limits are not in play at all | portability | speculative for the SQLite path; confirmed for the measurements and for the bind-parameter conclusion | `app/models/query.rb:1509` (`sql_contains` → `sanitize_sql_for_conditions`), `db/migrate/013_create_queries.rb:6` | **—** |
| nit | `version-subprojects` | F09 | `GET /queries/filter?c=foo` now raises `NoMethodError` on an endpoint anonymous users can reach | backward-compat | confirmed | `app/controllers/queries_controller.rb:103` | **—** |
| nit | `version-subprojects` | F10 | The branch is a month behind trunk, so the dossier's "RuboCop 0, baseline 0" no longer reproduces | dossier | confirmed | `docs/features/version-subprojects/dossier.md`, evidence block; `status.md`, "Bewijs" | **—** |
| nit | `version-subprojects` | F11 | The commit message carries a three-paragraph body; trunk commits are single-line | conventions | confirmed | commit `f4dd91175`, and therefore | **—** |
| nit | `webhook-issue-closed` | F06 | The transition table omits two cases that do fire `issue.closed`: a copy taken with `keep_status`, and every duplicate closed by cascade | dossier | confirmed | `docs/features/webhook-issue-closed/dossier.md`, "Which transitions fire, and which do not" | **—** |
| nit | `webhook-issue-closed` | F07 | The end-to-end test reads the first `WebhookJob` in the queue, not the job its own assertion block enqueued | test-quality | confirmed | `test/unit/webhook_test.rb:248` | **—** |
| nit | `webhook-issue-closed` | F08 | Passing the full event list means `Issue` stops inheriting future additions to `acts_as_webhookable`'s default | conventions | confirmed | `app/models/issue.rb:63` | **—** |
| nit | `webhook-issue-closed` | F09 | On an issue created directly in a closed status, `issue.closed` is enqueued before `issue.created` | correctness | confirmed | `app/models/concerns/issue/webhookable.rb:24` (registration order relative to `lib/redmine/acts/webhookable.rb:35`) | **—** |
| nit | `webhook-tracker-filter` | F08 | The tracker list is not scoped to the user, while the projects list next to it is | security | confirmed | `app/views/webhooks/_form.html.erb:45` | **—** |
| nit | `webhook-tracker-filter` | F09 | The four-clause condition in `hooks_for` is now a 152-character line | conventions | confirmed | `app/models/webhook.rb:133` | **—** |
| nit | `wiki-export-attachments` | F08 | `.sort_by(&:title)` is redundant and disagrees with the ordering the `pages` association already applies | minimality | confirmed | `app/controllers/wiki_controller.rb` — `wiki_page_directories`, `pages_by_parent_id.fetch(parent_id, []).sort_by(&:title)` | **—** |
| nit | `wiki-export-attachments` | F11 | An attachment filename of `..` reaches the archive as `<Page>/..`; page titles, by contrast, cannot escape at all | security | confirmed | `app/controllers/wiki_controller.rb` — `archived_attachment_filename`; `Attachment#sanitize_filename` | **—** |
| nit | `wiki-export-attachments` | F12 | The comment above `wiki_page_directories` half restates what the method does | conventions | confirmed | `app/controllers/wiki_controller.rb`, immediately above `def wiki_page_directories` | **—** |
| question | `ar-sessions` | Q01 | The commit's committer is `Claude <noreply@anthropic.com>` (INV-4), which traps.md records as knowingly not fixed | conventions | confirmed | commit `95bbb9750` metadata | **—** |
| question | `assignee-nobody` | F09 | question for Jan: is turning a 500 into a wrong answer on a neighbouring filter acceptable, or does F01 have to be fixed before the note goes up? | correctness | confirmed | `see F01` | **—** |
| question | `gitignore-credentials` | F03 | Ignoring `config/credentials.yml.enc` inverts Rails' intended workflow — is that deliberate? | minimality | confirmed | `.gitignore:11` | **—** |
| question | `ldap-mail-prefs` | F11 | For an account nobody uses, is `only_assigned` the value you want, or `none`? | scope | confirmed (the behaviour; the intent is yours) | `lib/tasks/disable_mail_ldap_users.rake:29` | **—** |
| question | `members-pagination` | Q01 | The three commits GEOxyz will run carry `Committer: Claude <noreply@anthropic.com>` | conventions | confirmed | `351fe9e54`, `55ae9d1dd`, `885f04097` on `7.0-stable-GEOxyz` | **—** |
| question | `mypage-query-blocks` | F10 | No upper bound: 999999 is accepted, and the cost is linear in the number of blocks | settings-surface | confirmed | `config/settings.yml:93-95`; `lib/redmine/my_page.rb:71-73` | **—** |
| question | `revision-branches` | F11 | Question for Jan: bound the per-revision command before submitting, or let the core team ask? | performance | n/a | the F02 measurement; `docs/features/revision-branches/status.md`, "Wat er al bekend is"; `docs/DECISIONS.md` (2026-09-01) | **—** |
| question | `search-token-limit` | F09 | question for Jan (settled): the cap boundary is visible in two places K-04 did not weigh | scope | n/a | `docs/DECISIONS.md`, K-04 (2026-09-02, option A) | **—** |
| question | `version-subprojects` | F12 | Question for Jan: should the note lead with the regression, or with the credit? | dossier | n/a | `docs/features/version-subprojects/status.md`, "Wat Jan nog moet doen" | **—** |
| question | `webhook-issue-closed` | F10 | Should the `closed_on` guard carry a one-line "why"? | conventions | n/a | `app/models/concerns/issue/webhookable.rb:24` | **—** |
| question | `webhook-tracker-filter` | F10 | An issue that moves out of a selected tracker produces no event, so the receiver goes stale | correctness | confirmed | `app/models/webhook.rb:140` (`matches_tracker?` reads the object's current `tracker_id`) | **—** |
| question | `webhook-tracker-filter` | F11 | A forged `tracker_ids` value produces a 500, not a validation error (settled) | security | confirmed | `app/controllers/webhooks_controller.rb:67` | **—** |
| question | `wiki-export-attachments` | F09 | Wiki-page attachments go into the archive without the container-level visibility check that every other bulk download applies | security | confirmed | `app/controllers/wiki_controller.rb` — `wiki_page_attachments`; compare `app/controllers/attachments_controller.rb` — `find_downloadable_attachments` | **—** |
| question | `wiki-export-attachments` | F10 | ~85 lines of archive construction now live in `WikiController` rather than under `lib/redmine/export/` | conventions | confirmed | `app/controllers/wiki_controller.rb` — `wiki_pages_to_zip`, `wiki_page_directories`, `zip_entry`, `archived_attachment_filename`, `wiki_page_attachments`, `wiki_attachments_too_big?` | **—** |

## Per review

| Slug | Datum | Reviewer | Counts | Suite gedraaid |
|---|---|---|---|---|
| [`ar-sessions`](findings/2026-09-03-ar-sessions-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 1 / major 3 / minor 6 / nit 1 / question 1 | yes, partly — `test/unit test/functional test/integration` i |
| [`assignee-nobody`](findings/2026-09-03-assignee-nobody-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 1 / minor 4 / nit 2 / question 1 | yes, partially. |
| [`geoxyz-hosts`](findings/2026-09-03-geoxyz-hosts-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 0 / minor 1 / nit 2 / question 0 | no. `config/environments/development.rb` is not loaded under |
| [`gitignore-credentials`](findings/2026-09-03-gitignore-credentials-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 1 / minor 1 / nit 0 / question 1 | no — `.gitignore` is not code and no suite reads it. Instead |
| [`imap-oauth`](findings/2026-09-03-imap-oauth-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 1 / minor 3 / nit 0 / question 0 | yes, partially — the two new files together in one process |
| [`ldap-mail-prefs`](findings/2026-09-03-ldap-mail-prefs-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 2 / major 3 / minor 3 / nit 2 / question 1 | no. There is no test to run: the file is `lib/tasks/**`, whi |
| [`members-pagination`](findings/2026-09-03-members-pagination-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 3 / minor 5 / nit 1 / question 1 | yes. Built a throwaway worktree at r24882, applied |
| [`mypage-query-blocks`](findings/2026-09-03-mypage-query-blocks-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 1 / major 2 / minor 4 / nit 2 / question 1 | yes — touched suites only, in a throwaway PostgreSQL worktre |
| [`revision-branches`](findings/2026-09-03-revision-branches-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 3 / minor 5 / nit 2 / question 1 | yes — the five touched files, twice: once on the patch as au |
| [`search-token-limit`](findings/2026-09-03-search-token-limit-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 3 / minor 2 / nit 3 / question 1 | yes, partially, and several experiments of my own: |
| [`version-subprojects`](findings/2026-09-03-version-subprojects-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 2 / minor 5 / nit 3 / question 1 | yes — the touched suites in one process, plus a wider run, p |
| [`webhook-issue-closed`](findings/2026-09-03-webhook-issue-closed-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 0 / minor 5 / nit 4 / question 1 | yes — in a throwaway worktree at the patch tip, PostgreSQL 1 |
| [`webhook-tracker-filter`](findings/2026-09-03-webhook-tracker-filter-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 0 / major 2 / minor 5 / nit 2 / question 2 | yes — `test/unit` + `test/functional` on the patch branch in |
| [`wiki-export-attachments`](findings/2026-09-03-wiki-export-attachments-claude-opus5.md) | 2026-09-03 | claude-opus5 | blocker 2 / major 1 / minor 4 / nit 3 / question 2 | yes — throwaway worktree at `origin/master` `bee32a926` + bo |
